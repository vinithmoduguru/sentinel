{
  "name": "Fallback Circuit Path",
  "description": "Simulate circuit open so fallback path is used.",
  "cases": [
    {
      "id": "circuit-open",
      "category": "fallback_path",
      "input": { "triage": { "alertId": 2006, "customerId": 42, "simulate": { "riskSignals": "circuit_open" } } },
      "expected": { "fallback_used": true }
    }
  ]
}


